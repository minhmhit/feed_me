# feed_me

Next.js version of the donation landing page, ready for fast deploy on Vercel.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The donate flow now uses the local QR image at [src/Khay_Qr.jpg](src/Khay_Qr.jpg) instead of the broken external links.
